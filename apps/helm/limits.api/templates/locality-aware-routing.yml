---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: limits-dest-rule2
  namespace: limits
spec:
  host: limits-ms-base.limits
  trafficPolicy:
    connectionPool:
      http:
        http2MaxRequests: 10
        maxRequestsPerConnection: 10
    outlierDetection:
      consecutive5xxErrors: 1
      interval: 20s
      baseEjectionTime: 30s